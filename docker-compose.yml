version: '3.8'
services:
  text-editor:
    image: nginx:alpine
    container_name: text-editor
    ports:
      - "80:80"
    volumes:
      - ./docs:/usr/share/nginx/html:ro
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
      - NET_BIND_SERVICE
    restart: unless-stopped
